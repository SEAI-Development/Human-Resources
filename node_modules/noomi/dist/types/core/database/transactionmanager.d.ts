import { UnknownClass } from "../types/other";
export declare class TransactionManager {
    /**
     * 事务切点名
     */
    private static pointcutId;
    /**
     * 添加事务Aop操作
     * @param clazz -       事务类
     * @param methodName -  方法名或方法名数组，支持通配符'*'
     */
    static addTransactionAop(clazz: UnknownClass, methodName: string | string[]): void;
    /**
     * 初始化事务 advice
     */
    static initAdvice(): void;
}
