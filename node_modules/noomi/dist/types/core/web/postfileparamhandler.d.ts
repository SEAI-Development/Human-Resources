/// <reference types="node" />
/**
 * 针对post不同mime进行处理
 */
export declare class PostFileHandler {
    /**
     * 文件保存目录
     */
    private saveDir;
    /**
     * 返回对象
     */
    returnObj: object;
    /**
     * 文件绝对路径
     */
    private path;
    constructor(tmpDir: string, mimeType: string);
    /**
     * 处理buf
     * @param buf -  新缓冲区
     */
    handleBuf(buf: Buffer): void;
    /**
     * 获取result
     * @returns     路径对象
     */
    getResult(): {
        path: string;
    };
}
