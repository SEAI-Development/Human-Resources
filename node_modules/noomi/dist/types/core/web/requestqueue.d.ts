import { HttpRequest } from "./httprequest";
/**
 * 请求队列
 */
export declare class RequestQueue {
    /**
     * 资源访问
     * @param request -   request
     */
    static handle(request: HttpRequest): Promise<void>;
    /**
     * 处理结果
     * @param response -  response对象
     * @param data -      数据或数据对象
     * @param path -      资源路径
     * @param isZip -     是否zip
     */
    private static handleResult;
}
